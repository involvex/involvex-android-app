{
  "name": "involvex-monorepo",
  "version": "0.0.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "android": "bun --cwd packages/app run android",
    "ios": "bun --cwd packages/app run ios",
    "web": "bun --cwd packages/web run dev",
    "api": "bun --cwd packages/api run dev",
    "build:android": "bun --cwd packages/app run build:android",
    "build:web": "bun --cwd packages/web run build",
    "build:api": "bun --cwd packages/api run deploy",
    "format": "bun --cwd packages/app run format && bun --cwd packages/web run format",
    "lint": "bun run lint:app && bun run lint:api",
    "lint:app": "bun --cwd packages/app run lint",
    "lint:api": "bun --cwd packages/api run lint",
    "typecheck": "bun run typecheck:app && bun run typecheck:shared && bun run typecheck:api",
    "typecheck:app": "bun --cwd packages/app run typecheck",
    "typecheck:shared": "bun --cwd packages/shared run typecheck",
    "typecheck:api": "bun --cwd packages/api run typecheck",
    "db:create": "bun --cwd packages/database run create",
    "db:migrate": "bun --cwd packages/database run migrate",
    "deploy:api": "bun --cwd packages/api run deploy",
    "deploy:web": "bun --cwd packages/web run deploy",
    "deploy:db": "bun --cwd packages/database run deploy"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.0.0"
  }
}
